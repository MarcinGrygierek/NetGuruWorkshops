%h1 Listing products

%table.table.table-bordered
  %tr
    %th Title
    %th Description
    %th Price
    %th Action

  - products.each do |product|
    -if product.category.name == category.name
      %tr
        %td= product.title
        %td= product.description
        %td= product.price
        %td
          = link_to 'Show', category_product_path(product.category, product),  class: 'btn btn-sm btn-primary'
          - if current_user == product.user or current_user.admin?
            = link_to 'Edit', edit_category_product_path(product.category, product),  class: 'btn btn-sm btn-primary'
            = link_to 'Destroy', category_product_path(product.category, product), method: :delete, data: { confirm: 'Are you sure?' },  class: 'btn btn-sm btn-primary'

%br
= link_to 'Back', categories_path, class: 'btn btn-primary'
